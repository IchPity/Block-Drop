{
  "name": "block-drop",
  "version": "0.12.0",
  "private": true,
  "description": "Block Drop — statisches Frontend auf Cloudflare Workers + Static-Assets",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "javascript-obfuscator": "4.2.2",
    "puppeteer-core": "25.1.0",
    "wrangler": "4.100.0"
  }
}
