.claude/
.idea/
Schach/.idea/
Schach/out/

# Cloudflare / Wrangler
.wrangler/
node_modules/
.dev.vars

# Secrets — NUR lokal, NIEMALS committen (DB-Admin-Keys, Connection-Strings, …)
.env
.env.*
.secrets
.secrets.*

# Windows-Kram
desktop.ini

# Lose Dateien, die nicht ins Repo sollen
kurt_converted.jpg

# Impeccable-Hook-Cache: session-lokale Edit-Historie übers ganze Repo,
# kein Projekt-Zustand (im Gegensatz zu .impeccable/config.json + critique/)
.impeccable/hook.cache.json

# Private Diplomarbeits-Unterlagen (Unterschriften, personenbezogene Daten) —
# lokal referenziert, aber nichts für ein öffentliches Repo
DiplData/
