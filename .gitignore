.claude/
.idea/

# Cloudflare / Wrangler
.wrangler/
node_modules/
.dev.vars

# Secrets — NUR lokal, NIEMALS committen (DB-Admin-Keys, Connection-Strings, …)
.env
.env.*
.secrets
.secrets.*

# Windows-Kram
desktop.ini

# Lose Dateien, die nicht ins Repo sollen
kurt_converted.jpg

# Private Diplomarbeits-Unterlagen (Unterschriften, personenbezogene Daten) —
# lokal referenziert, aber nichts für ein öffentliches Repo
DiplData/
